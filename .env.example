# SSH Deployment (primary)
# Copy to .env and fill in. Repo must be cloned on server at SSH_REMOTE_PATH.
SSH_HOST=92.112.189.73
SSH_PORT=65002
SSH_USER=u660126262
SSH_PASSWORD=your-password
SSH_REMOTE_PATH=/path/to/repo/on/server
# SSH_GIT_BRANCH=master

# FTP (for static deploy: upload out/ to document root)
FTP_HOST=your-ftp-host
FTP_USER=your-ftp-username
FTP_PASSWORD=your-ftp-password
# Document root on host (e.g. public_html). Leave unset to upload to FTP root.
# FTP_REMOTE_DIR=public_html

# Static export: forms need an external endpoint (no /api/contact). Optional:
# Create forms at https://formspree.io and set:
# NEXT_PUBLIC_CONTACT_FORM_ACTION=https://formspree.io/f/your-contact-form-id
# NEXT_PUBLIC_BOOKING_FORM_ACTION=https://formspree.io/f/your-booking-form-id

# Optional: where booking/contact messages should be sent (used by API if you run Node)
CONTACT_TO_EMAIL=your-email@example.com

# Optional: simple shared secret if you later add a mail provider
CONTACT_API_KEY=
